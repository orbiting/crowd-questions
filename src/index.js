import React, { Fragment } from 'react'
import PropTypes from 'prop-types'

import CrowdQuestions from './components/CrowdQuestions'

const Wrapper = (props) => {
  return <CrowdQuestions {...props} />
}

export default Wrapper
